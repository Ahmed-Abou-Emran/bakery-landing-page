import styled from "styled-components";

export const Heading2 = styled.h2`
  text-transform: uppercase;
  color: var(--brown-500);
  text-align: center;
  font-family: Sen;
  font-size: clamp(2.1rem, 0.72rem + 6.9vw, 9rem);
  font-weight: 800;
  line-height: 0.96;
`;
